/*using System;

namespace oneIntro
{
    class Operaciones
    {
        //Atributos (propiedades)
        public      int         numeroUno                  { get; set; }
        //mod acc   tipo dato   nombrePropieda (atributo)  metodos para recibir (set) y para entregar (get) el valor de la propiedad
        public int numeroDos { get; set; }        

        //Metodos (funciones)
        /*public    void         nuestraSuma(int n1, int n2)
        {
        //mod acc  tipoRetorno nombreMetodo(args o param)
            int suma = n1 + n2;
            Console.WriteLine($"La suma de {n1} y {n2} es {suma}");
        } 

        //separar responsabilidades en los metodos
        public int calcularSuma(int n1, int n2)
        {
            int resultadoSuma = 0;

            resultadoSuma = n1 + n2;

            return resultadoSuma;
        }        

        public void imprimirSuma(){

            int resultadoGeneral = calcularSuma(numeroUno, numeroDos);

            Console.WriteLine(
                "La suma es " + resultadoGeneral;

                //calcularSuma(numeroUno, numeroDos)
            );
        }

    }
} */